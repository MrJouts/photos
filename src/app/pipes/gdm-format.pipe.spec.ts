import { GdmFormatPipe } from './gdm-format.pipe';

describe('GdmFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new GdmFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
